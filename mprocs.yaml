procs:
  inngest:
    cmd: ["powershell", "-Command", "inngest-cli dev"]

  next:
    cmd: ["powershell", "-Command", "npm run dev"]

  prisma-studio:
    cmd: ["powershell", "-Command", "npx prisma studio"]
